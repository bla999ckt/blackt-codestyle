{
  "name": "blackt-codestyle",
  "displayName": "BlackT Code Style",
  "description": "VS Code extension to apply custom code styles.",
  "version": "1.0.6",
  "publisher": "bla999ckt",
  "icon": "images/icon.png",
  "license": "MIT",
  "engines": {
    "vscode": "^1.96.0"
  },
  "bin": {
    "codestyle": "./codestyle-cli.js"
  },
  "activationEvents": [
    "onCommand:codestyle",
    "onCommand:applyCodeStyle"
  ],
  "main": "./extension.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/bla999ckt/blackt-codestyle.git"
  },
  "contributes": {
    "commands": [
      {
        "command": "blackt-codestyle.applyCodeStyle",
        "title": "Apply Code Style"
      }
    ],
    "views": {
      "explorer": [
        {
          "id": "style50Explorer",
          "name": "Code Style"
        }
      ]
    }
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "vscode": "^1.96.0"
  },
  "scripts": {
    "package": "vsce package",
    "compile": "echo 'No compilation necessary'",
    "vscode:prepublish": "npm run compile",
    "build": "esbuild ./extension.js --bundle --outfile=out/extension.js --external:vscode"
  },
  "dependencies": {
    "black": "^0.3.0",
    "clang-format": "^1.8.0",
    "prettier": "^3.4.2"
}
}
